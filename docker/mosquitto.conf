# Local-dev Mosquitto config for the meshcore-rpc-services test bench.
# Open access — do NOT use this in production.

listener 1883
allow_anonymous true

persistence true
persistence_location /mosquitto/data/
autosave_on_changes true
autosave_interval 30

log_dest stdout
log_type error
log_type warning
log_type notice
